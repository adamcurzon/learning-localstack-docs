# learning-localstack-docs

Docusaurus documentation for the [learning-localstack](https://github.com/adamcurzon/learning-localstack) AWS SAM and LocalStack project.

## Local development

```bash
npm install
npm run start
```

Build the site for production:

```bash
npm run build
```

The generated site is written to `build/`.
